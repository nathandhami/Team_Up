mixin event-panel(collapseNum, teamupName, from, to, sport, locationName, locationAddress, aliasId, count, mixinId)
  .panel.panel-default.ac-event-panel(id = mixinId)
    .panel-heading.container-fluid
      .row
        h5.panel-title
          .col-md-4
            a(data-toggle="collapse", href= '#' + collapseNum)
              span= teamupName
          .col-md-6
            span#status
          div#participants.col-md-2.text-right
            abbr(title="Participants")
              <i class="fa fa-user"> #{count}/10  </i>
    div.panel-collapse.collapse.in(id=collapseNum)
      .panel-body
        p= 'Sport: ' + sport
        p= 'Location: ' + locationName
        p= 'Address: '  + locationAddress
        p= 'From: ' + from
        p= 'To: ' + to
        div
          button.joinEventBtn.btn.btn-success(role="button", href='#') Join Event
            input(type='hidden', value=aliasId)
