After vagrant up, site can be accessed on http://localhost:8080/

Google and Twitter authentication along with local authentication works. Everything else is work in progress.
In the 'Create Sports Event' tab, the google maps works, the buttons to select the sport works, and the form is still in progress.
As for the 'Edit Account', it will render a new page and the rest is work in progress.